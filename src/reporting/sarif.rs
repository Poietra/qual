//! SARIF 2.1.0 rendering (DESIGN §8.1, Phase 5).
//!
//! TODO(phase-5): generate SARIF 2.1.0 without external dependencies;
//! `--format sarif` reports a clear "not implemented" error until then.

/// Placeholder for the SARIF renderer.
#[derive(Debug, Default, Clone, Copy)]
#[non_exhaustive]
pub struct SarifRenderer {}
