"""Benchmark fixture file 16.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene16_00(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(DOWN * 2)
        mob1 = Square(color=RED)
        mob1.shift(UP * 2)
        mob2 = Circle(color=PURPLE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 16-00 step 48")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_01(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(DOWN * 3)
        mob1 = Dot(color=BLUE)
        mob1.shift(DOWN * 3)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(LEFT * 1)
        mob3 = Circle(color=RED)
        mob3.shift(RIGHT * 1)
        title = Text("scene 16-01 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_02(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Triangle(color=GREEN)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=RED)
        mob2.shift(UP * 3)
        mob3 = Triangle(color=BLUE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 16-02 step 95")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_03(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Star(color=ORANGE)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=GREEN)
        mob2.shift(RIGHT * 2)
        mob3 = Triangle(color=ORANGE)
        mob3.shift(LEFT * 2)
        title = Text("scene 16-03 step 39")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_04(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=BLUE)
        mob2.shift(UP * 2)
        title = Text("scene 16-04 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_05(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(LEFT * 2)
        mob3 = Square(color=YELLOW)
        mob3.shift(DOWN * 3)
        title = Text("scene 16-05 step 8")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_06(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=RED)
        mob1.shift(RIGHT * 1)
        mob2 = Circle(color=ORANGE)
        mob2.shift(RIGHT * 3)
        title = Text("scene 16-06 step 50")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_07(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(UP * 1)
        mob1 = Square(color=YELLOW)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=ORANGE)
        mob2.shift(DOWN * 2)
        mob3 = Rectangle(color=BLUE)
        mob3.shift(UP * 2)
        title = Text("scene 16-07 step 93")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_08(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(LEFT * 3)
        mob1 = Star(color=RED)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 16-08 step 8")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_09(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(RIGHT * 3)
        mob3 = Square(color=RED)
        mob3.shift(RIGHT * 3)
        title = Text("scene 16-09 step 72")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_10(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=BLUE)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(LEFT * 1)
        title = Text("scene 16-10 step 59")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_11(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(UP * 2)
        mob1 = Dot(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=RED)
        mob2.shift(UP * 2)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(DOWN * 1)
        title = Text("scene 16-11 step 62")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_12(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(RIGHT * 1)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(LEFT * 1)
        title = Text("scene 16-12 step 57")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_13(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Square(color=PURPLE)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=GREEN)
        mob2.shift(DOWN * 2)
        title = Text("scene 16-13 step 78")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_14(Scene):
    def construct(self):
        mob0 = Triangle(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Square(color=YELLOW)
        mob2.shift(LEFT * 2)
        title = Text("scene 16-14 step 96")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_15(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(LEFT * 3)
        mob1 = Dot(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Circle(color=YELLOW)
        mob3.shift(DOWN * 1)
        title = Text("scene 16-15 step 66")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_16(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Triangle(color=YELLOW)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(DOWN * 2)
        mob3 = Star(color=PURPLE)
        mob3.shift(DOWN * 1)
        title = Text("scene 16-16 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_17(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(UP * 3)
        mob1 = Dot(color=GREEN)
        mob1.shift(LEFT * 2)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(DOWN * 3)
        mob3 = Square(color=GREEN)
        mob3.shift(RIGHT * 1)
        title = Text("scene 16-17 step 96")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_18(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(DOWN * 2)
        mob2 = Circle(color=YELLOW)
        mob2.shift(UP * 1)
        title = Text("scene 16-18 step 98")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_19(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=BLUE)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=PURPLE)
        mob2.shift(DOWN * 1)
        title = Text("scene 16-19 step 51")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene16_20(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=GREEN)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=YELLOW)
        mob2.shift(RIGHT * 3)
        title = Text("scene 16-20 step 93")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
