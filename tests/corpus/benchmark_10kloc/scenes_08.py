"""Benchmark fixture file 08.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene08_00(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(LEFT * 1)
        mob1 = Dot(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(LEFT * 2)
        title = Text("scene 08-00 step 83")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_01(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(LEFT * 1)
        mob1 = Dot(color=ORANGE)
        mob1.shift(DOWN * 1)
        mob2 = Star(color=ORANGE)
        mob2.shift(RIGHT * 1)
        mob3 = Rectangle(color=PURPLE)
        mob3.shift(DOWN * 2)
        title = Text("scene 08-01 step 25")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_02(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(UP * 1)
        mob1 = Star(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Rectangle(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(UP * 1)
        title = Text("scene 08-02 step 96")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_03(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(UP * 2)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Square(color=ORANGE)
        mob2.shift(DOWN * 2)
        mob3 = Square(color=BLUE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 08-03 step 3")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_04(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(RIGHT * 2)
        mob1 = Star(color=ORANGE)
        mob1.shift(UP * 1)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(RIGHT * 3)
        title = Text("scene 08-04 step 23")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_05(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(RIGHT * 3)
        mob1 = Star(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=YELLOW)
        mob2.shift(DOWN * 1)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 08-05 step 77")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_06(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(LEFT * 2)
        mob1 = Square(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Circle(color=BLUE)
        mob2.shift(DOWN * 3)
        title = Text("scene 08-06 step 98")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_07(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(DOWN * 1)
        mob1 = Circle(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=BLUE)
        mob2.shift(DOWN * 1)
        mob3 = Star(color=GREEN)
        mob3.shift(LEFT * 3)
        title = Text("scene 08-07 step 38")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_08(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(UP * 1)
        mob1 = Dot(color=BLUE)
        mob1.shift(LEFT * 3)
        mob2 = Square(color=YELLOW)
        mob2.shift(RIGHT * 1)
        mob3 = Dot(color=BLUE)
        mob3.shift(DOWN * 3)
        title = Text("scene 08-08 step 61")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_09(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(RIGHT * 2)
        mob1 = Star(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Square(color=BLUE)
        mob2.shift(RIGHT * 3)
        title = Text("scene 08-09 step 79")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_10(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(DOWN * 2)
        mob1 = Dot(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=GREEN)
        mob2.shift(RIGHT * 2)
        title = Text("scene 08-10 step 22")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_11(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=BLUE)
        mob2.shift(DOWN * 3)
        title = Text("scene 08-11 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_12(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(UP * 1)
        mob1 = Square(color=GREEN)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=BLUE)
        mob2.shift(UP * 3)
        title = Text("scene 08-12 step 3")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_13(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(DOWN * 2)
        mob1 = Square(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 08-13 step 50")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_14(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(DOWN * 1)
        mob1 = Rectangle(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=YELLOW)
        mob2.shift(DOWN * 1)
        title = Text("scene 08-14 step 87")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_15(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(UP * 3)
        mob1 = Dot(color=BLUE)
        mob1.shift(RIGHT * 2)
        mob2 = Circle(color=RED)
        mob2.shift(UP * 3)
        title = Text("scene 08-15 step 6")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_16(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=BLUE)
        mob2.shift(DOWN * 2)
        title = Text("scene 08-16 step 55")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_17(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(RIGHT * 1)
        mob1 = Square(color=GREEN)
        mob1.shift(UP * 3)
        mob2 = Star(color=GREEN)
        mob2.shift(UP * 1)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 08-17 step 70")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_18(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Square(color=RED)
        mob1.shift(DOWN * 3)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(RIGHT * 3)
        mob3 = Square(color=RED)
        mob3.shift(RIGHT * 3)
        title = Text("scene 08-18 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_19(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(UP * 3)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(RIGHT * 3)
        mob3 = Circle(color=PURPLE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 08-19 step 55")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene08_20(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 08-20 step 80")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
