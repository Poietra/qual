"""Benchmark fixture file 18.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene18_00(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=YELLOW)
        mob1.shift(UP * 3)
        mob2 = Dot(color=ORANGE)
        mob2.shift(LEFT * 1)
        title = Text("scene 18-00 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_01(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Triangle(color=GREEN)
        mob1.shift(UP * 1)
        mob2 = Triangle(color=BLUE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 18-01 step 22")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_02(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Dot(color=GREEN)
        mob2.shift(RIGHT * 2)
        title = Text("scene 18-02 step 27")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_03(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Star(color=BLUE)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(RIGHT * 3)
        title = Text("scene 18-03 step 23")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_04(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(UP * 1)
        mob1 = Square(color=BLUE)
        mob1.shift(UP * 2)
        mob2 = Square(color=YELLOW)
        mob2.shift(DOWN * 3)
        title = Text("scene 18-04 step 6")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_05(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=ORANGE)
        mob1.shift(UP * 1)
        mob2 = Circle(color=GREEN)
        mob2.shift(UP * 1)
        title = Text("scene 18-05 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_06(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(DOWN * 3)
        mob1 = Dot(color=RED)
        mob1.shift(UP * 1)
        mob2 = Triangle(color=BLUE)
        mob2.shift(RIGHT * 3)
        mob3 = Star(color=ORANGE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 18-06 step 16")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_07(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Star(color=RED)
        mob1.shift(UP * 2)
        mob2 = Star(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 18-07 step 11")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_08(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Square(color=BLUE)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=ORANGE)
        mob2.shift(LEFT * 3)
        mob3 = Dot(color=PURPLE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 18-08 step 91")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_09(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Star(color=PURPLE)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(RIGHT * 2)
        mob3 = Star(color=ORANGE)
        mob3.shift(LEFT * 3)
        title = Text("scene 18-09 step 3")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_10(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(DOWN * 2)
        mob3 = Star(color=GREEN)
        mob3.shift(RIGHT * 2)
        title = Text("scene 18-10 step 45")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_11(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=BLUE)
        mob1.shift(UP * 3)
        mob2 = Triangle(color=BLUE)
        mob2.shift(LEFT * 1)
        mob3 = Circle(color=PURPLE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 18-11 step 23")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_12(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(RIGHT * 2)
        mob1 = Star(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(RIGHT * 1)
        mob3 = Star(color=PURPLE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 18-12 step 69")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_13(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(UP * 3)
        mob1 = Dot(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(DOWN * 2)
        mob3 = Triangle(color=BLUE)
        mob3.shift(LEFT * 3)
        title = Text("scene 18-13 step 92")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_14(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=PURPLE)
        mob1.shift(UP * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(LEFT * 2)
        mob3 = Circle(color=ORANGE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 18-14 step 49")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_15(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(LEFT * 3)
        mob1 = Star(color=RED)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(DOWN * 1)
        title = Text("scene 18-15 step 92")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_16(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(LEFT * 3)
        mob1 = Dot(color=GREEN)
        mob1.shift(UP * 1)
        mob2 = Dot(color=ORANGE)
        mob2.shift(RIGHT * 1)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(UP * 1)
        title = Text("scene 18-16 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_17(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=GREEN)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=YELLOW)
        mob2.shift(DOWN * 3)
        title = Text("scene 18-17 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_18(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(RIGHT * 2)
        mob1 = Circle(color=ORANGE)
        mob1.shift(UP * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(LEFT * 1)
        mob3 = Dot(color=GREEN)
        mob3.shift(DOWN * 3)
        title = Text("scene 18-18 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_19(Scene):
    def construct(self):
        mob0 = Triangle(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Rectangle(color=GREEN)
        mob1.shift(RIGHT * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(LEFT * 3)
        title = Text("scene 18-19 step 44")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene18_20(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(DOWN * 3)
        mob1 = Circle(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(LEFT * 1)
        title = Text("scene 18-20 step 44")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
