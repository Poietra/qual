"""Benchmark fixture file 13.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene13_00(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(LEFT * 1)
        mob1 = Star(color=GREEN)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(DOWN * 3)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(UP * 3)
        title = Text("scene 13-00 step 21")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_01(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(UP * 2)
        mob1 = Circle(color=RED)
        mob1.shift(DOWN * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(UP * 2)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(RIGHT * 3)
        title = Text("scene 13-01 step 94")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_02(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=BLUE)
        mob1.shift(UP * 3)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(LEFT * 3)
        mob3 = Rectangle(color=BLUE)
        mob3.shift(UP * 2)
        title = Text("scene 13-02 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_03(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(RIGHT * 3)
        mob1 = Square(color=RED)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=YELLOW)
        mob2.shift(LEFT * 2)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(LEFT * 2)
        title = Text("scene 13-03 step 2")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_04(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(UP * 3)
        mob1 = Dot(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 2)
        title = Text("scene 13-04 step 67")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_05(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(RIGHT * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(DOWN * 2)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(UP * 2)
        title = Text("scene 13-05 step 30")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_06(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=ORANGE)
        mob2.shift(UP * 1)
        mob3 = Star(color=PURPLE)
        mob3.shift(UP * 3)
        title = Text("scene 13-06 step 74")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_07(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(UP * 3)
        mob1 = Circle(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=YELLOW)
        mob2.shift(DOWN * 1)
        mob3 = Star(color=PURPLE)
        mob3.shift(LEFT * 3)
        title = Text("scene 13-07 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_08(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(RIGHT * 2)
        mob1 = Circle(color=BLUE)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=GREEN)
        mob2.shift(UP * 3)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 13-08 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_09(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(RIGHT * 3)
        mob1 = Rectangle(color=RED)
        mob1.shift(DOWN * 2)
        mob2 = Circle(color=ORANGE)
        mob2.shift(LEFT * 3)
        title = Text("scene 13-09 step 73")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_10(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(UP * 3)
        mob1 = Square(color=GREEN)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=RED)
        mob2.shift(DOWN * 2)
        mob3 = Circle(color=PURPLE)
        mob3.shift(LEFT * 3)
        title = Text("scene 13-10 step 69")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_11(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=GREEN)
        mob1.shift(RIGHT * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 13-11 step 5")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_12(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(LEFT * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=GREEN)
        mob2.shift(LEFT * 3)
        title = Text("scene 13-12 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_13(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Star(color=YELLOW)
        mob1.shift(DOWN * 1)
        mob2 = Circle(color=YELLOW)
        mob2.shift(DOWN * 2)
        title = Text("scene 13-13 step 96")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_14(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Dot(color=RED)
        mob1.shift(LEFT * 1)
        mob2 = Circle(color=GREEN)
        mob2.shift(UP * 3)
        mob3 = Triangle(color=YELLOW)
        mob3.shift(RIGHT * 1)
        title = Text("scene 13-14 step 36")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_15(Scene):
    def construct(self):
        mob0 = Circle(color=YELLOW)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(LEFT * 3)
        title = Text("scene 13-15 step 48")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_16(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Star(color=PURPLE)
        mob2.shift(UP * 3)
        mob3 = Triangle(color=BLUE)
        mob3.shift(LEFT * 3)
        title = Text("scene 13-16 step 26")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_17(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(DOWN * 2)
        mob1 = Dot(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=GREEN)
        mob2.shift(LEFT * 3)
        mob3 = Triangle(color=RED)
        mob3.shift(LEFT * 3)
        title = Text("scene 13-17 step 27")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_18(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=GREEN)
        mob1.shift(DOWN * 1)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(UP * 2)
        mob3 = Square(color=RED)
        mob3.shift(DOWN * 2)
        title = Text("scene 13-18 step 93")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_19(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=BLUE)
        mob2.shift(DOWN * 3)
        title = Text("scene 13-19 step 26")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene13_20(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(UP * 3)
        mob1 = Square(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Circle(color=PURPLE)
        mob2.shift(DOWN * 3)
        mob3 = Square(color=YELLOW)
        mob3.shift(DOWN * 1)
        title = Text("scene 13-20 step 51")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
