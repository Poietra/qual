"""Benchmark fixture file 14.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene14_00(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(DOWN * 1)
        mob2 = Rectangle(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 14-00 step 88")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_01(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(UP * 2)
        title = Text("scene 14-01 step 37")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_02(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Star(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Square(color=PURPLE)
        mob2.shift(DOWN * 2)
        mob3 = Square(color=PURPLE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 14-02 step 79")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_03(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(RIGHT * 3)
        mob1 = Triangle(color=GREEN)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=RED)
        mob2.shift(RIGHT * 3)
        title = Text("scene 14-03 step 28")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_04(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=GREEN)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=RED)
        mob2.shift(RIGHT * 2)
        title = Text("scene 14-04 step 15")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_05(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(DOWN * 1)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(UP * 3)
        title = Text("scene 14-05 step 45")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_06(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=ORANGE)
        mob2.shift(UP * 3)
        mob3 = Triangle(color=GREEN)
        mob3.shift(DOWN * 1)
        title = Text("scene 14-06 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_07(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=BLUE)
        mob1.shift(DOWN * 3)
        mob2 = Triangle(color=RED)
        mob2.shift(RIGHT * 2)
        mob3 = Dot(color=RED)
        mob3.shift(LEFT * 3)
        title = Text("scene 14-07 step 69")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_08(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=PURPLE)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(LEFT * 1)
        title = Text("scene 14-08 step 36")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_09(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Star(color=BLUE)
        mob1.shift(UP * 3)
        mob2 = Star(color=PURPLE)
        mob2.shift(DOWN * 3)
        mob3 = Dot(color=GREEN)
        mob3.shift(DOWN * 2)
        title = Text("scene 14-09 step 18")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_10(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Square(color=ORANGE)
        mob1.shift(UP * 3)
        mob2 = Dot(color=RED)
        mob2.shift(LEFT * 1)
        mob3 = Triangle(color=YELLOW)
        mob3.shift(RIGHT * 2)
        title = Text("scene 14-10 step 89")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_11(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(RIGHT * 2)
        mob3 = Circle(color=PURPLE)
        mob3.shift(UP * 2)
        title = Text("scene 14-11 step 53")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_12(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(DOWN * 3)
        mob1 = Circle(color=BLUE)
        mob1.shift(UP * 3)
        mob2 = Triangle(color=BLUE)
        mob2.shift(DOWN * 1)
        title = Text("scene 14-12 step 28")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_13(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(UP * 1)
        mob1 = Square(color=BLUE)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=RED)
        mob2.shift(UP * 3)
        title = Text("scene 14-13 step 60")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_14(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Star(color=BLUE)
        mob2.shift(DOWN * 1)
        title = Text("scene 14-14 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_15(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=GREEN)
        mob2.shift(DOWN * 2)
        mob3 = Rectangle(color=PURPLE)
        mob3.shift(DOWN * 1)
        title = Text("scene 14-15 step 16")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_16(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=PURPLE)
        mob2.shift(UP * 3)
        mob3 = Dot(color=RED)
        mob3.shift(RIGHT * 1)
        title = Text("scene 14-16 step 53")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_17(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=PURPLE)
        mob2.shift(DOWN * 1)
        title = Text("scene 14-17 step 79")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_18(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(DOWN * 1)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=BLUE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 14-18 step 86")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_19(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(DOWN * 3)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(DOWN * 1)
        mob2 = Circle(color=ORANGE)
        mob2.shift(UP * 3)
        title = Text("scene 14-19 step 15")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene14_20(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=PURPLE)
        mob2.shift(UP * 1)
        mob3 = Star(color=RED)
        mob3.shift(LEFT * 2)
        title = Text("scene 14-20 step 13")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
