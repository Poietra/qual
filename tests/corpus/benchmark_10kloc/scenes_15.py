"""Benchmark fixture file 15.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene15_00(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(UP * 2)
        mob2 = Square(color=RED)
        mob2.shift(DOWN * 2)
        mob3 = Square(color=RED)
        mob3.shift(UP * 1)
        title = Text("scene 15-00 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_01(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 3)
        mob3 = Square(color=YELLOW)
        mob3.shift(LEFT * 1)
        title = Text("scene 15-01 step 22")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_02(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(DOWN * 1)
        mob1 = Circle(color=YELLOW)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=GREEN)
        mob2.shift(LEFT * 1)
        mob3 = Circle(color=ORANGE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 15-02 step 42")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_03(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(RIGHT * 2)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(UP * 3)
        mob2 = Rectangle(color=GREEN)
        mob2.shift(DOWN * 1)
        title = Text("scene 15-03 step 62")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_04(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=GREEN)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=GREEN)
        mob2.shift(DOWN * 2)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(LEFT * 1)
        title = Text("scene 15-04 step 2")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_05(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(UP * 1)
        mob3 = Circle(color=ORANGE)
        mob3.shift(DOWN * 3)
        title = Text("scene 15-05 step 37")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_06(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(LEFT * 1)
        mob3 = Rectangle(color=RED)
        mob3.shift(UP * 3)
        title = Text("scene 15-06 step 45")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_07(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(LEFT * 3)
        mob1 = Square(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(LEFT * 2)
        title = Text("scene 15-07 step 77")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_08(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Square(color=GREEN)
        mob1.shift(RIGHT * 1)
        mob2 = Circle(color=ORANGE)
        mob2.shift(UP * 1)
        mob3 = Circle(color=PURPLE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 15-08 step 40")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_09(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(RIGHT * 1)
        mob1 = Circle(color=GREEN)
        mob1.shift(RIGHT * 2)
        mob2 = Dot(color=ORANGE)
        mob2.shift(LEFT * 2)
        mob3 = Triangle(color=ORANGE)
        mob3.shift(LEFT * 3)
        title = Text("scene 15-09 step 60")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_10(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(UP * 3)
        mob1 = Circle(color=YELLOW)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=GREEN)
        mob2.shift(RIGHT * 1)
        title = Text("scene 15-10 step 17")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_11(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(DOWN * 3)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(LEFT * 1)
        mob3 = Square(color=RED)
        mob3.shift(LEFT * 3)
        title = Text("scene 15-11 step 80")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_12(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=ORANGE)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=PURPLE)
        mob2.shift(UP * 3)
        mob3 = Circle(color=YELLOW)
        mob3.shift(RIGHT * 1)
        title = Text("scene 15-12 step 83")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_13(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(LEFT * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(RIGHT * 3)
        title = Text("scene 15-13 step 6")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_14(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(LEFT * 2)
        mob1 = Rectangle(color=RED)
        mob1.shift(DOWN * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(UP * 2)
        title = Text("scene 15-14 step 20")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_15(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=PURPLE)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=YELLOW)
        mob2.shift(UP * 2)
        title = Text("scene 15-15 step 11")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_16(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(RIGHT * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(UP * 1)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(LEFT * 3)
        title = Text("scene 15-16 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_17(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(LEFT * 2)
        mob1 = Square(color=RED)
        mob1.shift(UP * 1)
        mob2 = Star(color=YELLOW)
        mob2.shift(UP * 1)
        mob3 = Star(color=BLUE)
        mob3.shift(LEFT * 2)
        title = Text("scene 15-17 step 12")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_18(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(LEFT * 3)
        mob1 = Dot(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Circle(color=YELLOW)
        mob2.shift(LEFT * 2)
        title = Text("scene 15-18 step 6")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_19(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(LEFT * 3)
        mob1 = Square(color=YELLOW)
        mob1.shift(DOWN * 2)
        mob2 = Square(color=PURPLE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 15-19 step 67")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene15_20(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=BLUE)
        mob1.shift(RIGHT * 2)
        mob2 = Triangle(color=BLUE)
        mob2.shift(DOWN * 3)
        title = Text("scene 15-20 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
