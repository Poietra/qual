"""Benchmark fixture file 04.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene04_00(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(DOWN * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=YELLOW)
        mob2.shift(RIGHT * 2)
        mob3 = Triangle(color=RED)
        mob3.shift(RIGHT * 2)
        title = Text("scene 04-00 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_01(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(DOWN * 3)
        mob1 = Star(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=GREEN)
        mob2.shift(RIGHT * 1)
        title = Text("scene 04-01 step 11")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_02(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=RED)
        mob1.shift(RIGHT * 1)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(LEFT * 1)
        title = Text("scene 04-02 step 26")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_03(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(UP * 2)
        mob1 = Circle(color=PURPLE)
        mob1.shift(DOWN * 3)
        mob2 = Triangle(color=RED)
        mob2.shift(RIGHT * 1)
        title = Text("scene 04-03 step 95")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_04(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Star(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=YELLOW)
        mob2.shift(LEFT * 1)
        mob3 = Star(color=YELLOW)
        mob3.shift(UP * 1)
        title = Text("scene 04-04 step 32")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_05(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(UP * 2)
        mob1 = Dot(color=GREEN)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=ORANGE)
        mob2.shift(UP * 2)
        title = Text("scene 04-05 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_06(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(RIGHT * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=ORANGE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 04-06 step 60")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_07(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(DOWN * 3)
        mob1 = Dot(color=GREEN)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=PURPLE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 04-07 step 99")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_08(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(UP * 3)
        mob1 = Square(color=YELLOW)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(LEFT * 1)
        title = Text("scene 04-08 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_09(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Dot(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(RIGHT * 2)
        title = Text("scene 04-09 step 25")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_10(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Rectangle(color=RED)
        mob2.shift(UP * 3)
        mob3 = Star(color=BLUE)
        mob3.shift(DOWN * 3)
        title = Text("scene 04-10 step 26")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_11(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=ORANGE)
        mob1.shift(RIGHT * 1)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(DOWN * 2)
        title = Text("scene 04-11 step 76")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_12(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(UP * 2)
        mob2 = Square(color=BLUE)
        mob2.shift(RIGHT * 1)
        mob3 = Dot(color=RED)
        mob3.shift(RIGHT * 1)
        title = Text("scene 04-12 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_13(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(UP * 3)
        mob1 = Dot(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Circle(color=BLUE)
        mob2.shift(LEFT * 1)
        title = Text("scene 04-13 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_14(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Square(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=GREEN)
        mob2.shift(LEFT * 2)
        title = Text("scene 04-14 step 20")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_15(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(LEFT * 2)
        mob1 = Square(color=ORANGE)
        mob1.shift(UP * 2)
        mob2 = Circle(color=RED)
        mob2.shift(LEFT * 1)
        title = Text("scene 04-15 step 72")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_16(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(LEFT * 2)
        mob1 = Star(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=PURPLE)
        mob2.shift(RIGHT * 1)
        mob3 = Star(color=ORANGE)
        mob3.shift(LEFT * 2)
        title = Text("scene 04-16 step 30")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_17(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(RIGHT * 1)
        mob1 = Square(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=PURPLE)
        mob2.shift(LEFT * 2)
        title = Text("scene 04-17 step 56")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_18(Scene):
    def construct(self):
        mob0 = Triangle(color=BLUE)
        mob0.shift(DOWN * 3)
        mob1 = Triangle(color=GREEN)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=BLUE)
        mob2.shift(UP * 3)
        title = Text("scene 04-18 step 27")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_19(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(UP * 1)
        mob1 = Star(color=YELLOW)
        mob1.shift(RIGHT * 3)
        mob2 = Square(color=ORANGE)
        mob2.shift(LEFT * 1)
        title = Text("scene 04-19 step 20")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene04_20(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Square(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(UP * 1)
        title = Text("scene 04-20 step 54")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
