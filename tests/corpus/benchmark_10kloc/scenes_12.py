"""Benchmark fixture file 12.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene12_00(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=RED)
        mob1.shift(DOWN * 1)
        mob2 = Star(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 12-00 step 13")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_01(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Circle(color=BLUE)
        mob2.shift(RIGHT * 3)
        mob3 = Rectangle(color=BLUE)
        mob3.shift(LEFT * 3)
        title = Text("scene 12-01 step 41")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_02(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Rectangle(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Square(color=PURPLE)
        mob2.shift(UP * 3)
        mob3 = Dot(color=BLUE)
        mob3.shift(LEFT * 1)
        title = Text("scene 12-02 step 52")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_03(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(UP * 1)
        mob1 = Star(color=RED)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 3)
        title = Text("scene 12-03 step 19")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_04(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=GREEN)
        mob1.shift(RIGHT * 2)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(DOWN * 2)
        title = Text("scene 12-04 step 38")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_05(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(DOWN * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(RIGHT * 2)
        mob2 = Dot(color=ORANGE)
        mob2.shift(UP * 1)
        title = Text("scene 12-05 step 8")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_06(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=GREEN)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=ORANGE)
        mob2.shift(DOWN * 3)
        title = Text("scene 12-06 step 5")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_07(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=YELLOW)
        mob1.shift(UP * 2)
        mob2 = Dot(color=PURPLE)
        mob2.shift(DOWN * 2)
        title = Text("scene 12-07 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_08(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=RED)
        mob2.shift(UP * 2)
        title = Text("scene 12-08 step 8")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_09(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=PURPLE)
        mob1.shift(UP * 3)
        mob2 = Star(color=PURPLE)
        mob2.shift(UP * 2)
        mob3 = Triangle(color=ORANGE)
        mob3.shift(DOWN * 2)
        title = Text("scene 12-09 step 2")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_10(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=RED)
        mob1.shift(RIGHT * 1)
        mob2 = Square(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 12-10 step 2")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_11(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=GREEN)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=GREEN)
        mob2.shift(LEFT * 2)
        title = Text("scene 12-11 step 47")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_12(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(UP * 1)
        mob3 = Square(color=GREEN)
        mob3.shift(UP * 2)
        title = Text("scene 12-12 step 49")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_13(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Square(color=BLUE)
        mob2.shift(LEFT * 1)
        mob3 = Circle(color=GREEN)
        mob3.shift(LEFT * 3)
        title = Text("scene 12-13 step 42")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_14(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(UP * 2)
        mob1 = Square(color=YELLOW)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=ORANGE)
        mob2.shift(DOWN * 2)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(UP * 2)
        title = Text("scene 12-14 step 41")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_15(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(RIGHT * 2)
        mob1 = Circle(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(LEFT * 2)
        title = Text("scene 12-15 step 78")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_16(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(LEFT * 3)
        mob1 = Triangle(color=RED)
        mob1.shift(DOWN * 3)
        mob2 = Circle(color=YELLOW)
        mob2.shift(LEFT * 3)
        title = Text("scene 12-16 step 88")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_17(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=GREEN)
        mob2.shift(LEFT * 3)
        title = Text("scene 12-17 step 56")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_18(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Star(color=ORANGE)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=RED)
        mob2.shift(DOWN * 3)
        title = Text("scene 12-18 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_19(Scene):
    def construct(self):
        mob0 = Circle(color=YELLOW)
        mob0.shift(RIGHT * 3)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Dot(color=ORANGE)
        mob2.shift(LEFT * 2)
        title = Text("scene 12-19 step 72")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene12_20(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(LEFT * 3)
        mob1 = Star(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(LEFT * 3)
        mob3 = Triangle(color=GREEN)
        mob3.shift(UP * 2)
        title = Text("scene 12-20 step 32")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
