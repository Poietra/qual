"""Benchmark fixture file 03.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene03_00(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=YELLOW)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=RED)
        mob2.shift(UP * 2)
        mob3 = Rectangle(color=BLUE)
        mob3.shift(LEFT * 2)
        title = Text("scene 03-00 step 9")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_01(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=ORANGE)
        mob1.shift(DOWN * 1)
        mob2 = Square(color=ORANGE)
        mob2.shift(LEFT * 3)
        mob3 = Rectangle(color=RED)
        mob3.shift(UP * 2)
        title = Text("scene 03-01 step 42")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_02(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=YELLOW)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=PURPLE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 03-02 step 86")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_03(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(DOWN * 3)
        mob1 = Triangle(color=GREEN)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=GREEN)
        mob2.shift(DOWN * 2)
        title = Text("scene 03-03 step 7")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_04(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=RED)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(RIGHT * 1)
        mob3 = Triangle(color=YELLOW)
        mob3.shift(LEFT * 3)
        title = Text("scene 03-04 step 90")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_05(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=YELLOW)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=RED)
        mob2.shift(UP * 3)
        mob3 = Circle(color=RED)
        mob3.shift(DOWN * 1)
        title = Text("scene 03-05 step 1")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_06(Scene):
    def construct(self):
        mob0 = Circle(color=YELLOW)
        mob0.shift(RIGHT * 1)
        mob1 = Star(color=RED)
        mob1.shift(LEFT * 3)
        mob2 = Dot(color=BLUE)
        mob2.shift(UP * 3)
        title = Text("scene 03-06 step 2")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_07(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Dot(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Triangle(color=BLUE)
        mob2.shift(LEFT * 3)
        title = Text("scene 03-07 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_08(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(UP * 2)
        mob1 = Star(color=YELLOW)
        mob1.shift(LEFT * 2)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(DOWN * 3)
        mob3 = Triangle(color=YELLOW)
        mob3.shift(RIGHT * 2)
        title = Text("scene 03-08 step 7")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_09(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Star(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(RIGHT * 2)
        mob3 = Star(color=GREEN)
        mob3.shift(DOWN * 3)
        title = Text("scene 03-09 step 7")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_10(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(RIGHT * 1)
        mob1 = Circle(color=RED)
        mob1.shift(UP * 2)
        mob2 = Rectangle(color=RED)
        mob2.shift(UP * 1)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(LEFT * 1)
        title = Text("scene 03-10 step 70")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_11(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=GREEN)
        mob1.shift(LEFT * 3)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 1)
        mob3 = Circle(color=YELLOW)
        mob3.shift(UP * 3)
        title = Text("scene 03-11 step 15")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_12(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(DOWN * 2)
        mob1 = Dot(color=RED)
        mob1.shift(UP * 3)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(DOWN * 2)
        mob3 = Rectangle(color=BLUE)
        mob3.shift(UP * 3)
        title = Text("scene 03-12 step 99")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_13(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(UP * 3)
        mob1 = Star(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Rectangle(color=GREEN)
        mob2.shift(DOWN * 3)
        mob3 = Dot(color=BLUE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 03-13 step 56")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_14(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Triangle(color=GREEN)
        mob1.shift(UP * 1)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(DOWN * 3)
        mob3 = Star(color=YELLOW)
        mob3.shift(LEFT * 1)
        title = Text("scene 03-14 step 74")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_15(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(LEFT * 3)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(DOWN * 1)
        mob2 = Triangle(color=ORANGE)
        mob2.shift(UP * 1)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(UP * 3)
        title = Text("scene 03-15 step 50")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_16(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(UP * 1)
        mob1 = Star(color=YELLOW)
        mob1.shift(LEFT * 2)
        mob2 = Square(color=GREEN)
        mob2.shift(DOWN * 2)
        mob3 = Circle(color=RED)
        mob3.shift(LEFT * 1)
        title = Text("scene 03-16 step 9")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_17(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(UP * 2)
        mob1 = Square(color=RED)
        mob1.shift(UP * 2)
        mob2 = Square(color=PURPLE)
        mob2.shift(UP * 3)
        title = Text("scene 03-17 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_18(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(LEFT * 3)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=GREEN)
        mob2.shift(LEFT * 1)
        title = Text("scene 03-18 step 31")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_19(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(LEFT * 3)
        mob1 = Star(color=YELLOW)
        mob1.shift(DOWN * 3)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(DOWN * 2)
        title = Text("scene 03-19 step 20")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene03_20(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(DOWN * 3)
        mob2 = Square(color=ORANGE)
        mob2.shift(RIGHT * 2)
        mob3 = Star(color=GREEN)
        mob3.shift(RIGHT * 3)
        title = Text("scene 03-20 step 2")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
