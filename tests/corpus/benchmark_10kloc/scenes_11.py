"""Benchmark fixture file 11.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene11_00(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Rectangle(color=GREEN)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=BLUE)
        mob2.shift(LEFT * 3)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(LEFT * 2)
        title = Text("scene 11-00 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_01(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(DOWN * 3)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(RIGHT * 1)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(UP * 3)
        mob3 = Circle(color=BLUE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 11-01 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_02(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(DOWN * 1)
        mob1 = Star(color=ORANGE)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=GREEN)
        mob2.shift(UP * 3)
        mob3 = Star(color=RED)
        mob3.shift(RIGHT * 1)
        title = Text("scene 11-02 step 79")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_03(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(UP * 2)
        mob1 = Circle(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Circle(color=YELLOW)
        mob2.shift(DOWN * 2)
        title = Text("scene 11-03 step 39")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_04(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Square(color=GREEN)
        mob1.shift(DOWN * 1)
        mob2 = Star(color=RED)
        mob2.shift(UP * 1)
        mob3 = Square(color=PURPLE)
        mob3.shift(UP * 3)
        title = Text("scene 11-04 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_05(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Dot(color=GREEN)
        mob2.shift(LEFT * 2)
        mob3 = Star(color=BLUE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 11-05 step 20")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_06(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 2)
        mob3 = Square(color=ORANGE)
        mob3.shift(UP * 3)
        title = Text("scene 11-06 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_07(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Triangle(color=YELLOW)
        mob1.shift(UP * 1)
        mob2 = Triangle(color=GREEN)
        mob2.shift(LEFT * 2)
        title = Text("scene 11-07 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_08(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(LEFT * 3)
        mob1 = Triangle(color=RED)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=RED)
        mob2.shift(DOWN * 1)
        title = Text("scene 11-08 step 68")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_09(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Triangle(color=GREEN)
        mob1.shift(RIGHT * 1)
        mob2 = Rectangle(color=GREEN)
        mob2.shift(DOWN * 3)
        title = Text("scene 11-09 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_10(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=GREEN)
        mob1.shift(UP * 3)
        mob2 = Square(color=ORANGE)
        mob2.shift(UP * 2)
        mob3 = Dot(color=BLUE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 11-10 step 47")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_11(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(UP * 2)
        mob1 = Dot(color=YELLOW)
        mob1.shift(UP * 1)
        mob2 = Circle(color=BLUE)
        mob2.shift(DOWN * 3)
        title = Text("scene 11-11 step 57")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_12(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=RED)
        mob1.shift(UP * 3)
        mob2 = Star(color=YELLOW)
        mob2.shift(LEFT * 3)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(UP * 1)
        title = Text("scene 11-12 step 74")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_13(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=RED)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=RED)
        mob2.shift(RIGHT * 3)
        title = Text("scene 11-13 step 67")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_14(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(RIGHT * 2)
        mob1 = Circle(color=GREEN)
        mob1.shift(DOWN * 1)
        mob2 = Square(color=PURPLE)
        mob2.shift(UP * 1)
        title = Text("scene 11-14 step 36")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_15(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=BLUE)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=PURPLE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 11-15 step 56")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_16(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=BLUE)
        mob1.shift(LEFT * 3)
        mob2 = Square(color=PURPLE)
        mob2.shift(DOWN * 3)
        title = Text("scene 11-16 step 60")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_17(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(DOWN * 1)
        mob1 = Star(color=YELLOW)
        mob1.shift(UP * 1)
        mob2 = Square(color=RED)
        mob2.shift(RIGHT * 1)
        title = Text("scene 11-17 step 86")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_18(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(UP * 2)
        mob1 = Circle(color=YELLOW)
        mob1.shift(RIGHT * 1)
        mob2 = Rectangle(color=RED)
        mob2.shift(LEFT * 2)
        title = Text("scene 11-18 step 9")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_19(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=PURPLE)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=RED)
        mob2.shift(RIGHT * 3)
        mob3 = Circle(color=ORANGE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 11-19 step 55")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene11_20(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(UP * 3)
        mob1 = Dot(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Square(color=BLUE)
        mob2.shift(LEFT * 2)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(UP * 3)
        title = Text("scene 11-20 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
