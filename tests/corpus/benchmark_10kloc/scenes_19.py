"""Benchmark fixture file 19.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene19_00(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(DOWN * 2)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(UP * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 1)
        mob3 = Star(color=BLUE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 19-00 step 78")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_01(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=RED)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=GREEN)
        mob2.shift(RIGHT * 3)
        title = Text("scene 19-01 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_02(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(DOWN * 3)
        mob1 = Star(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(DOWN * 3)
        mob3 = Star(color=YELLOW)
        mob3.shift(LEFT * 3)
        title = Text("scene 19-02 step 50")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_03(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=PURPLE)
        mob1.shift(UP * 3)
        mob2 = Star(color=ORANGE)
        mob2.shift(UP * 2)
        mob3 = Star(color=GREEN)
        mob3.shift(UP * 2)
        title = Text("scene 19-03 step 86")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_04(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(DOWN * 3)
        mob1 = Dot(color=YELLOW)
        mob1.shift(RIGHT * 1)
        mob2 = Circle(color=BLUE)
        mob2.shift(DOWN * 2)
        title = Text("scene 19-04 step 12")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_05(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(RIGHT * 3)
        mob1 = Square(color=PURPLE)
        mob1.shift(DOWN * 2)
        mob2 = Rectangle(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Star(color=BLUE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 19-05 step 73")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_06(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=RED)
        mob1.shift(DOWN * 2)
        mob2 = Star(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Circle(color=BLUE)
        mob3.shift(DOWN * 1)
        title = Text("scene 19-06 step 94")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_07(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(DOWN * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(UP * 2)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(RIGHT * 1)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(LEFT * 1)
        title = Text("scene 19-07 step 59")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_08(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=PURPLE)
        mob2.shift(UP * 2)
        mob3 = Square(color=YELLOW)
        mob3.shift(LEFT * 2)
        title = Text("scene 19-08 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_09(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(RIGHT * 1)
        mob1 = Square(color=RED)
        mob1.shift(UP * 3)
        mob2 = Star(color=RED)
        mob2.shift(RIGHT * 3)
        title = Text("scene 19-09 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_10(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=PURPLE)
        mob2.shift(DOWN * 2)
        mob3 = Square(color=YELLOW)
        mob3.shift(RIGHT * 3)
        title = Text("scene 19-10 step 39")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_11(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=PURPLE)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Star(color=GREEN)
        mob3.shift(RIGHT * 2)
        title = Text("scene 19-11 step 83")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_12(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=YELLOW)
        mob1.shift(LEFT * 3)
        mob2 = Square(color=BLUE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 19-12 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_13(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=RED)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=YELLOW)
        mob2.shift(LEFT * 3)
        mob3 = Dot(color=GREEN)
        mob3.shift(RIGHT * 1)
        title = Text("scene 19-13 step 70")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_14(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(UP * 1)
        mob1 = Dot(color=PURPLE)
        mob1.shift(DOWN * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(RIGHT * 3)
        mob3 = Dot(color=ORANGE)
        mob3.shift(LEFT * 1)
        title = Text("scene 19-14 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_15(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=ORANGE)
        mob1.shift(LEFT * 2)
        mob2 = Dot(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(UP * 3)
        title = Text("scene 19-15 step 45")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_16(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(DOWN * 2)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(DOWN * 1)
        mob2 = Rectangle(color=GREEN)
        mob2.shift(DOWN * 2)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(RIGHT * 2)
        title = Text("scene 19-16 step 7")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_17(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(UP * 3)
        mob1 = Dot(color=ORANGE)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=ORANGE)
        mob2.shift(UP * 1)
        title = Text("scene 19-17 step 49")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_18(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 19-18 step 29")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_19(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(RIGHT * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(UP * 1)
        title = Text("scene 19-19 step 82")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene19_20(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(UP * 2)
        mob1 = Square(color=GREEN)
        mob1.shift(DOWN * 2)
        mob2 = Circle(color=ORANGE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 19-20 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
