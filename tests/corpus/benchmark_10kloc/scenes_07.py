"""Benchmark fixture file 07.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene07_00(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=BLUE)
        mob2.shift(RIGHT * 2)
        mob3 = Dot(color=YELLOW)
        mob3.shift(LEFT * 2)
        title = Text("scene 07-00 step 31")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_01(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(RIGHT * 2)
        mob1 = Rectangle(color=RED)
        mob1.shift(DOWN * 1)
        mob2 = Circle(color=BLUE)
        mob2.shift(LEFT * 2)
        title = Text("scene 07-01 step 73")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_02(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(RIGHT * 2)
        mob3 = Rectangle(color=RED)
        mob3.shift(LEFT * 2)
        title = Text("scene 07-02 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_03(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=RED)
        mob2.shift(RIGHT * 3)
        title = Text("scene 07-03 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_04(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(UP * 3)
        title = Text("scene 07-04 step 39")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_05(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=YELLOW)
        mob1.shift(UP * 2)
        mob2 = Square(color=ORANGE)
        mob2.shift(DOWN * 1)
        title = Text("scene 07-05 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_06(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=RED)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(LEFT * 1)
        title = Text("scene 07-06 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_07(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(UP * 2)
        mob1 = Square(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Circle(color=PURPLE)
        mob2.shift(RIGHT * 3)
        mob3 = Square(color=YELLOW)
        mob3.shift(DOWN * 1)
        title = Text("scene 07-07 step 40")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_08(Scene):
    def construct(self):
        mob0 = Circle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Star(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=ORANGE)
        mob2.shift(UP * 1)
        title = Text("scene 07-08 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_09(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(DOWN * 1)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=BLUE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 07-09 step 30")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_10(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Square(color=YELLOW)
        mob1.shift(DOWN * 2)
        mob2 = Circle(color=YELLOW)
        mob2.shift(UP * 3)
        mob3 = Circle(color=PURPLE)
        mob3.shift(DOWN * 2)
        title = Text("scene 07-10 step 74")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_11(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=BLUE)
        mob1.shift(RIGHT * 2)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(UP * 1)
        title = Text("scene 07-11 step 87")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_12(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Circle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=GREEN)
        mob2.shift(UP * 2)
        mob3 = Triangle(color=PURPLE)
        mob3.shift(LEFT * 2)
        title = Text("scene 07-12 step 27")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_13(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(UP * 3)
        mob1 = Dot(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=RED)
        mob2.shift(RIGHT * 2)
        mob3 = Star(color=GREEN)
        mob3.shift(DOWN * 2)
        title = Text("scene 07-13 step 52")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_14(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(UP * 2)
        mob1 = Star(color=BLUE)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=ORANGE)
        mob2.shift(LEFT * 3)
        mob3 = Circle(color=GREEN)
        mob3.shift(LEFT * 1)
        title = Text("scene 07-14 step 29")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_15(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(DOWN * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(UP * 1)
        mob2 = Dot(color=ORANGE)
        mob2.shift(RIGHT * 1)
        mob3 = Square(color=ORANGE)
        mob3.shift(LEFT * 2)
        title = Text("scene 07-15 step 46")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_16(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=BLUE)
        mob1.shift(LEFT * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(RIGHT * 1)
        mob3 = Circle(color=PURPLE)
        mob3.shift(LEFT * 1)
        title = Text("scene 07-16 step 55")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_17(Scene):
    def construct(self):
        mob0 = Circle(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Triangle(color=YELLOW)
        mob1.shift(UP * 2)
        mob2 = Star(color=GREEN)
        mob2.shift(RIGHT * 2)
        mob3 = Square(color=RED)
        mob3.shift(RIGHT * 1)
        title = Text("scene 07-17 step 68")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_18(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(RIGHT * 3)
        mob1 = Square(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Star(color=RED)
        mob2.shift(LEFT * 3)
        title = Text("scene 07-18 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_19(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Star(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Circle(color=PURPLE)
        mob2.shift(DOWN * 2)
        title = Text("scene 07-19 step 89")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene07_20(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Square(color=GREEN)
        mob2.shift(UP * 1)
        title = Text("scene 07-20 step 68")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
