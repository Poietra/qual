"""Benchmark fixture file 10.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene10_00(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(LEFT * 3)
        mob1 = Dot(color=GREEN)
        mob1.shift(DOWN * 1)
        mob2 = Star(color=ORANGE)
        mob2.shift(DOWN * 3)
        title = Text("scene 10-00 step 9")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_01(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Square(color=YELLOW)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=GREEN)
        mob2.shift(LEFT * 3)
        title = Text("scene 10-01 step 94")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_02(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(UP * 2)
        mob3 = Triangle(color=BLUE)
        mob3.shift(UP * 3)
        title = Text("scene 10-02 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_03(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=BLUE)
        mob1.shift(UP * 3)
        mob2 = Star(color=ORANGE)
        mob2.shift(RIGHT * 3)
        mob3 = Square(color=PURPLE)
        mob3.shift(LEFT * 3)
        title = Text("scene 10-03 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_04(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(LEFT * 1)
        mob1 = Star(color=BLUE)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 10-04 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_05(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 2)
        title = Text("scene 10-05 step 28")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_06(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(LEFT * 3)
        mob1 = Star(color=BLUE)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(RIGHT * 1)
        mob3 = Square(color=YELLOW)
        mob3.shift(UP * 1)
        title = Text("scene 10-06 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_07(Scene):
    def construct(self):
        mob0 = Rectangle(color=GREEN)
        mob0.shift(LEFT * 1)
        mob1 = Triangle(color=RED)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=YELLOW)
        mob2.shift(UP * 2)
        title = Text("scene 10-07 step 10")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_08(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(LEFT * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Star(color=BLUE)
        mob2.shift(UP * 1)
        title = Text("scene 10-08 step 29")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_09(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(LEFT * 1)
        mob1 = Star(color=RED)
        mob1.shift(RIGHT * 2)
        mob2 = Square(color=YELLOW)
        mob2.shift(LEFT * 2)
        title = Text("scene 10-09 step 86")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_10(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(UP * 1)
        mob1 = Star(color=RED)
        mob1.shift(RIGHT * 2)
        mob2 = Circle(color=BLUE)
        mob2.shift(UP * 2)
        mob3 = Dot(color=ORANGE)
        mob3.shift(DOWN * 2)
        title = Text("scene 10-10 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_11(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(RIGHT * 3)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(LEFT * 1)
        mob3 = Star(color=YELLOW)
        mob3.shift(UP * 3)
        title = Text("scene 10-11 step 48")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_12(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(RIGHT * 2)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(RIGHT * 3)
        title = Text("scene 10-12 step 55")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_13(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Square(color=RED)
        mob2.shift(DOWN * 2)
        title = Text("scene 10-13 step 5")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_14(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(UP * 1)
        mob1 = Circle(color=GREEN)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(UP * 1)
        mob3 = Star(color=PURPLE)
        mob3.shift(LEFT * 3)
        title = Text("scene 10-14 step 95")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_15(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(DOWN * 3)
        mob1 = Star(color=YELLOW)
        mob1.shift(UP * 2)
        mob2 = Circle(color=YELLOW)
        mob2.shift(DOWN * 3)
        mob3 = Circle(color=PURPLE)
        mob3.shift(LEFT * 3)
        title = Text("scene 10-15 step 17")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_16(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=YELLOW)
        mob1.shift(DOWN * 2)
        mob2 = Dot(color=YELLOW)
        mob2.shift(UP * 2)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(DOWN * 3)
        title = Text("scene 10-16 step 79")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_17(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(UP * 1)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(RIGHT * 1)
        mob2 = Dot(color=YELLOW)
        mob2.shift(UP * 1)
        mob3 = Circle(color=PURPLE)
        mob3.shift(UP * 2)
        title = Text("scene 10-17 step 8")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_18(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Circle(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Star(color=YELLOW)
        mob2.shift(RIGHT * 3)
        mob3 = Rectangle(color=PURPLE)
        mob3.shift(DOWN * 2)
        title = Text("scene 10-18 step 67")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_19(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(DOWN * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Circle(color=GREEN)
        mob2.shift(LEFT * 1)
        title = Text("scene 10-19 step 46")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene10_20(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Star(color=YELLOW)
        mob1.shift(LEFT * 2)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 10-20 step 6")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
