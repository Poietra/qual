"""Benchmark fixture file 01.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene01_00(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(UP * 1)
        mob1 = Square(color=GREEN)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=GREEN)
        mob2.shift(DOWN * 1)
        title = Text("scene 01-00 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_01(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=PURPLE)
        mob1.shift(RIGHT * 3)
        mob2 = Circle(color=BLUE)
        mob2.shift(UP * 2)
        title = Text("scene 01-01 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_02(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Square(color=GREEN)
        mob1.shift(DOWN * 2)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(DOWN * 1)
        title = Text("scene 01-02 step 7")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_03(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(RIGHT * 3)
        mob1 = Triangle(color=RED)
        mob1.shift(LEFT * 3)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(DOWN * 1)
        title = Text("scene 01-03 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_04(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(LEFT * 1)
        mob1 = Star(color=BLUE)
        mob1.shift(RIGHT * 2)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(RIGHT * 3)
        mob3 = Rectangle(color=ORANGE)
        mob3.shift(DOWN * 3)
        title = Text("scene 01-04 step 91")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_05(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(DOWN * 3)
        mob1 = Dot(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Dot(color=GREEN)
        mob2.shift(LEFT * 1)
        mob3 = Triangle(color=GREEN)
        mob3.shift(UP * 1)
        title = Text("scene 01-05 step 20")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_06(Scene):
    def construct(self):
        mob0 = Star(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=RED)
        mob1.shift(DOWN * 3)
        mob2 = Triangle(color=GREEN)
        mob2.shift(LEFT * 3)
        mob3 = Dot(color=PURPLE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 01-06 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_07(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Star(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Rectangle(color=YELLOW)
        mob2.shift(UP * 1)
        mob3 = Star(color=BLUE)
        mob3.shift(UP * 3)
        title = Text("scene 01-07 step 64")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_08(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=GREEN)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(UP * 1)
        mob3 = Dot(color=YELLOW)
        mob3.shift(DOWN * 2)
        title = Text("scene 01-08 step 30")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_09(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=YELLOW)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=RED)
        mob2.shift(LEFT * 1)
        title = Text("scene 01-09 step 59")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_10(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(DOWN * 1)
        mob1 = Star(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=GREEN)
        mob2.shift(RIGHT * 2)
        mob3 = Star(color=BLUE)
        mob3.shift(RIGHT * 2)
        title = Text("scene 01-10 step 51")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_11(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(UP * 1)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(RIGHT * 2)
        mob3 = Square(color=BLUE)
        mob3.shift(LEFT * 2)
        title = Text("scene 01-11 step 46")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_12(Scene):
    def construct(self):
        mob0 = Dot(color=GREEN)
        mob0.shift(UP * 3)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(DOWN * 3)
        mob2 = Circle(color=GREEN)
        mob2.shift(LEFT * 2)
        title = Text("scene 01-12 step 46")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_13(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(LEFT * 3)
        mob1 = Star(color=YELLOW)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(UP * 2)
        mob3 = Circle(color=BLUE)
        mob3.shift(RIGHT * 1)
        title = Text("scene 01-13 step 92")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_14(Scene):
    def construct(self):
        mob0 = Star(color=GREEN)
        mob0.shift(LEFT * 2)
        mob1 = Triangle(color=YELLOW)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=RED)
        mob2.shift(UP * 2)
        title = Text("scene 01-14 step 27")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_15(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(LEFT * 3)
        mob1 = Square(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Triangle(color=BLUE)
        mob2.shift(RIGHT * 2)
        title = Text("scene 01-15 step 68")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_16(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Star(color=BLUE)
        mob2.shift(DOWN * 1)
        mob3 = Square(color=YELLOW)
        mob3.shift(RIGHT * 3)
        title = Text("scene 01-16 step 41")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_17(Scene):
    def construct(self):
        mob0 = Triangle(color=BLUE)
        mob0.shift(UP * 3)
        mob1 = Circle(color=YELLOW)
        mob1.shift(LEFT * 2)
        mob2 = Square(color=ORANGE)
        mob2.shift(UP * 1)
        mob3 = Circle(color=PURPLE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 01-17 step 77")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_18(Scene):
    def construct(self):
        mob0 = Triangle(color=BLUE)
        mob0.shift(UP * 2)
        mob1 = Square(color=PURPLE)
        mob1.shift(RIGHT * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(DOWN * 2)
        title = Text("scene 01-18 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_19(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(RIGHT * 1)
        mob1 = Circle(color=BLUE)
        mob1.shift(UP * 1)
        mob2 = Star(color=ORANGE)
        mob2.shift(DOWN * 2)
        mob3 = Circle(color=GREEN)
        mob3.shift(LEFT * 3)
        title = Text("scene 01-19 step 83")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene01_20(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(DOWN * 3)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(DOWN * 3)
        mob3 = Square(color=ORANGE)
        mob3.shift(DOWN * 1)
        title = Text("scene 01-20 step 84")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
