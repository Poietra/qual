"""Benchmark fixture file 09.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene09_00(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(LEFT * 3)
        mob1 = Triangle(color=GREEN)
        mob1.shift(RIGHT * 3)
        mob2 = Circle(color=ORANGE)
        mob2.shift(DOWN * 1)
        mob3 = Rectangle(color=RED)
        mob3.shift(UP * 2)
        title = Text("scene 09-00 step 98")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_01(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(RIGHT * 3)
        mob1 = Dot(color=ORANGE)
        mob1.shift(LEFT * 3)
        mob2 = Dot(color=ORANGE)
        mob2.shift(LEFT * 3)
        title = Text("scene 09-01 step 85")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_02(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(LEFT * 3)
        mob1 = Rectangle(color=RED)
        mob1.shift(UP * 3)
        mob2 = Triangle(color=PURPLE)
        mob2.shift(DOWN * 2)
        mob3 = Circle(color=PURPLE)
        mob3.shift(LEFT * 2)
        title = Text("scene 09-02 step 15")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(LEFT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_03(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=BLUE)
        mob1.shift(DOWN * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 09-03 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_04(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(LEFT * 2)
        mob1 = Rectangle(color=RED)
        mob1.shift(DOWN * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(UP * 1)
        title = Text("scene 09-04 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_05(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Dot(color=PURPLE)
        mob1.shift(DOWN * 3)
        mob2 = Circle(color=RED)
        mob2.shift(RIGHT * 3)
        title = Text("scene 09-05 step 66")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_06(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Triangle(color=RED)
        mob2.shift(RIGHT * 2)
        mob3 = Dot(color=BLUE)
        mob3.shift(LEFT * 2)
        title = Text("scene 09-06 step 63")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_07(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=BLUE)
        mob1.shift(UP * 3)
        mob2 = Rectangle(color=GREEN)
        mob2.shift(RIGHT * 3)
        mob3 = Dot(color=BLUE)
        mob3.shift(RIGHT * 3)
        title = Text("scene 09-07 step 42")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_08(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Star(color=GREEN)
        mob1.shift(LEFT * 3)
        mob2 = Dot(color=BLUE)
        mob2.shift(RIGHT * 3)
        title = Text("scene 09-08 step 33")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_09(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=RED)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=RED)
        mob2.shift(UP * 2)
        title = Text("scene 09-09 step 69")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_10(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Rectangle(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(LEFT * 2)
        title = Text("scene 09-10 step 1")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_11(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(RIGHT * 1)
        mob1 = Circle(color=YELLOW)
        mob1.shift(RIGHT * 3)
        mob2 = Triangle(color=BLUE)
        mob2.shift(LEFT * 2)
        title = Text("scene 09-11 step 48")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_12(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(RIGHT * 1)
        mob1 = Square(color=RED)
        mob1.shift(RIGHT * 2)
        mob2 = Star(color=PURPLE)
        mob2.shift(UP * 2)
        title = Text("scene 09-12 step 35")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_13(Scene):
    def construct(self):
        mob0 = Square(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=PURPLE)
        mob1.shift(DOWN * 1)
        mob2 = Circle(color=BLUE)
        mob2.shift(DOWN * 2)
        title = Text("scene 09-13 step 44")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_14(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=GREEN)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=RED)
        mob2.shift(UP * 3)
        mob3 = Triangle(color=RED)
        mob3.shift(UP * 1)
        title = Text("scene 09-14 step 10")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_15(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Circle(color=PURPLE)
        mob2.shift(DOWN * 2)
        mob3 = Triangle(color=BLUE)
        mob3.shift(LEFT * 2)
        title = Text("scene 09-15 step 14")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_16(Scene):
    def construct(self):
        mob0 = Triangle(color=YELLOW)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(UP * 2)
        mob2 = Square(color=RED)
        mob2.shift(RIGHT * 3)
        mob3 = Triangle(color=GREEN)
        mob3.shift(RIGHT * 3)
        title = Text("scene 09-16 step 3")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob3.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_17(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(LEFT * 1)
        mob1 = Triangle(color=RED)
        mob1.shift(RIGHT * 1)
        mob2 = Square(color=GREEN)
        mob2.shift(RIGHT * 3)
        mob3 = Triangle(color=BLUE)
        mob3.shift(LEFT * 3)
        title = Text("scene 09-17 step 10")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_18(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=BLUE)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=GREEN)
        mob2.shift(RIGHT * 2)
        mob3 = Circle(color=BLUE)
        mob3.shift(LEFT * 1)
        title = Text("scene 09-18 step 58")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_19(Scene):
    def construct(self):
        mob0 = Dot(color=YELLOW)
        mob0.shift(RIGHT * 2)
        mob1 = Square(color=GREEN)
        mob1.shift(UP * 2)
        mob2 = Circle(color=PURPLE)
        mob2.shift(LEFT * 2)
        title = Text("scene 09-19 step 72")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene09_20(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=GREEN)
        mob2.shift(UP * 2)
        mob3 = Circle(color=PURPLE)
        mob3.shift(LEFT * 3)
        title = Text("scene 09-20 step 15")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
