"""Benchmark fixture file 06.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene06_00(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=GREEN)
        mob2.shift(UP * 1)
        mob3 = Triangle(color=ORANGE)
        mob3.shift(DOWN * 2)
        title = Text("scene 06-00 step 76")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_01(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(UP * 3)
        mob1 = Rectangle(color=PURPLE)
        mob1.shift(LEFT * 3)
        mob2 = Triangle(color=RED)
        mob2.shift(UP * 1)
        mob3 = Rectangle(color=PURPLE)
        mob3.shift(UP * 1)
        title = Text("scene 06-01 step 46")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_02(Scene):
    def construct(self):
        mob0 = Star(color=PURPLE)
        mob0.shift(RIGHT * 1)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(UP * 1)
        mob2 = Circle(color=ORANGE)
        mob2.shift(DOWN * 3)
        mob3 = Star(color=PURPLE)
        mob3.shift(UP * 2)
        title = Text("scene 06-02 step 68")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_03(Scene):
    def construct(self):
        mob0 = Square(color=RED)
        mob0.shift(UP * 1)
        mob1 = Star(color=GREEN)
        mob1.shift(RIGHT * 2)
        mob2 = Circle(color=GREEN)
        mob2.shift(RIGHT * 2)
        title = Text("scene 06-03 step 55")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_04(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(DOWN * 3)
        mob1 = Dot(color=GREEN)
        mob1.shift(RIGHT * 3)
        mob2 = Dot(color=YELLOW)
        mob2.shift(UP * 1)
        title = Text("scene 06-04 step 23")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_05(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(LEFT * 2)
        mob1 = Dot(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=BLUE)
        mob2.shift(LEFT * 1)
        title = Text("scene 06-05 step 60")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_06(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(LEFT * 1)
        mob1 = Square(color=RED)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(LEFT * 3)
        title = Text("scene 06-06 step 5")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_07(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(UP * 3)
        mob1 = Circle(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Triangle(color=YELLOW)
        mob2.shift(UP * 1)
        mob3 = Star(color=ORANGE)
        mob3.shift(DOWN * 3)
        title = Text("scene 06-07 step 14")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_08(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(LEFT * 2)
        mob1 = Rectangle(color=YELLOW)
        mob1.shift(LEFT * 1)
        mob2 = Star(color=BLUE)
        mob2.shift(LEFT * 1)
        title = Text("scene 06-08 step 3")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_09(Scene):
    def construct(self):
        mob0 = Dot(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(DOWN * 2)
        mob2 = Rectangle(color=BLUE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 06-09 step 30")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{12} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_10(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(UP * 1)
        mob1 = Circle(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=YELLOW)
        mob2.shift(RIGHT * 1)
        mob3 = Dot(color=RED)
        mob3.shift(UP * 3)
        title = Text("scene 06-10 step 71")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_11(Scene):
    def construct(self):
        mob0 = Star(color=YELLOW)
        mob0.shift(UP * 3)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Square(color=BLUE)
        mob2.shift(UP * 3)
        mob3 = Square(color=PURPLE)
        mob3.shift(UP * 2)
        title = Text("scene 06-11 step 36")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_12(Scene):
    def construct(self):
        mob0 = Star(color=ORANGE)
        mob0.shift(RIGHT * 2)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=BLUE)
        mob2.shift(UP * 1)
        mob3 = Triangle(color=BLUE)
        mob3.shift(LEFT * 1)
        title = Text("scene 06-12 step 94")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_13(Scene):
    def construct(self):
        mob0 = Rectangle(color=YELLOW)
        mob0.shift(DOWN * 1)
        mob1 = Square(color=GREEN)
        mob1.shift(RIGHT * 1)
        mob2 = Triangle(color=BLUE)
        mob2.shift(RIGHT * 1)
        title = Text("scene 06-13 step 8")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{11} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_14(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(UP * 3)
        mob1 = Circle(color=ORANGE)
        mob1.shift(RIGHT * 1)
        mob2 = Square(color=RED)
        mob2.shift(UP * 1)
        title = Text("scene 06-14 step 50")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(UP * 1), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_15(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(LEFT * 2)
        mob1 = Square(color=ORANGE)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=YELLOW)
        mob2.shift(UP * 2)
        title = Text("scene 06-15 step 24")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{3} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_16(Scene):
    def construct(self):
        mob0 = Star(color=BLUE)
        mob0.shift(LEFT * 3)
        mob1 = Rectangle(color=GREEN)
        mob1.shift(UP * 1)
        mob2 = Dot(color=GREEN)
        mob2.shift(DOWN * 3)
        title = Text("scene 06-16 step 44")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_17(Scene):
    def construct(self):
        mob0 = Rectangle(color=ORANGE)
        mob0.shift(LEFT * 3)
        mob1 = Square(color=RED)
        mob1.shift(LEFT * 1)
        mob2 = Star(color=BLUE)
        mob2.shift(RIGHT * 2)
        mob3 = Star(color=ORANGE)
        mob3.shift(UP * 3)
        title = Text("scene 06-17 step 26")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_18(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(DOWN * 1)
        mob1 = Dot(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=GREEN)
        mob2.shift(UP * 2)
        mob3 = Star(color=YELLOW)
        mob3.shift(RIGHT * 3)
        title = Text("scene 06-18 step 19")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_19(Scene):
    def construct(self):
        mob0 = Triangle(color=PURPLE)
        mob0.shift(LEFT * 2)
        mob1 = Triangle(color=ORANGE)
        mob1.shift(RIGHT * 3)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(RIGHT * 3)
        mob3 = Square(color=YELLOW)
        mob3.shift(RIGHT * 3)
        title = Text("scene 06-19 step 13")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{10} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene06_20(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(LEFT * 2)
        mob1 = Star(color=YELLOW)
        mob1.shift(RIGHT * 3)
        mob2 = Star(color=YELLOW)
        mob2.shift(UP * 2)
        title = Text("scene 06-20 step 7")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(Create(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
