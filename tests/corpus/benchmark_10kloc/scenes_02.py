"""Benchmark fixture file 02.

Deterministically generated by tests/benchmark_gate.rs; do not edit.
"""
from manim import *


class Scene02_00(Scene):
    def construct(self):
        mob0 = Dot(color=RED)
        mob0.shift(UP * 2)
        mob1 = Circle(color=YELLOW)
        mob1.shift(DOWN * 2)
        mob2 = Circle(color=BLUE)
        mob2.shift(DOWN * 3)
        title = Text("scene 02-00 step 19")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_01(Scene):
    def construct(self):
        mob0 = Square(color=PURPLE)
        mob0.shift(LEFT * 1)
        mob1 = Triangle(color=BLUE)
        mob1.shift(RIGHT * 1)
        mob2 = Circle(color=PURPLE)
        mob2.shift(LEFT * 2)
        mob3 = Square(color=RED)
        mob3.shift(DOWN * 2)
        title = Text("scene 02-01 step 47")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(DOWN * 2), run_time=1)
        self.play(mob2.animate.shift(UP * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_02(Scene):
    def construct(self):
        mob0 = Rectangle(color=RED)
        mob0.shift(UP * 2)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Circle(color=YELLOW)
        mob2.shift(LEFT * 3)
        mob3 = Dot(color=BLUE)
        mob3.shift(LEFT * 1)
        title = Text("scene 02-02 step 74")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(UP * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_03(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(RIGHT * 3)
        mob1 = Square(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Triangle(color=BLUE)
        mob2.shift(LEFT * 1)
        mob3 = Rectangle(color=BLUE)
        mob3.shift(DOWN * 2)
        title = Text("scene 02-03 step 79")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 2), run_time=2)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_04(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(RIGHT * 1)
        mob1 = Triangle(color=GREEN)
        mob1.shift(LEFT * 2)
        mob2 = Star(color=RED)
        mob2.shift(LEFT * 2)
        mob3 = Dot(color=BLUE)
        mob3.shift(UP * 1)
        title = Text("scene 02-04 step 21")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_05(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(DOWN * 2)
        mob1 = Circle(color=YELLOW)
        mob1.shift(RIGHT * 1)
        mob2 = Square(color=GREEN)
        mob2.shift(UP * 1)
        mob3 = Triangle(color=GREEN)
        mob3.shift(UP * 1)
        title = Text("scene 02-05 step 51")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{7} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_06(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(RIGHT * 2)
        mob1 = Dot(color=PURPLE)
        mob1.shift(UP * 1)
        mob2 = Square(color=ORANGE)
        mob2.shift(LEFT * 3)
        title = Text("scene 02-06 step 96")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_07(Scene):
    def construct(self):
        mob0 = Circle(color=PURPLE)
        mob0.shift(UP * 2)
        mob1 = Dot(color=GREEN)
        mob1.shift(UP * 1)
        mob2 = Rectangle(color=ORANGE)
        mob2.shift(DOWN * 3)
        title = Text("scene 02-07 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{15} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(UP * 2), run_time=1)
        self.play(mob1.animate.shift(DOWN * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 1), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_08(Scene):
    def construct(self):
        mob0 = Square(color=ORANGE)
        mob0.shift(RIGHT * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(LEFT * 1)
        mob2 = Dot(color=BLUE)
        mob2.shift(UP * 1)
        mob3 = Circle(color=ORANGE)
        mob3.shift(DOWN * 3)
        title = Text("scene 02-08 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{9} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(LEFT * 2), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(mob3.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_09(Scene):
    def construct(self):
        mob0 = Triangle(color=RED)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=YELLOW)
        mob1.shift(RIGHT * 1)
        mob2 = Star(color=ORANGE)
        mob2.shift(UP * 2)
        mob3 = Star(color=PURPLE)
        mob3.shift(DOWN * 2)
        title = Text("scene 02-09 step 75")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{5} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(DOWN * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_10(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(DOWN * 3)
        mob1 = Rectangle(color=BLUE)
        mob1.shift(UP * 1)
        mob2 = Dot(color=GREEN)
        mob2.shift(RIGHT * 1)
        mob3 = Dot(color=GREEN)
        mob3.shift(RIGHT * 1)
        title = Text("scene 02-10 step 43")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{13} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob3.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_11(Scene):
    def construct(self):
        mob0 = Dot(color=BLUE)
        mob0.shift(RIGHT * 3)
        mob1 = Star(color=PURPLE)
        mob1.shift(UP * 2)
        mob2 = Triangle(color=GREEN)
        mob2.shift(RIGHT * 1)
        mob3 = Rectangle(color=YELLOW)
        mob3.shift(LEFT * 3)
        title = Text("scene 02-11 step 16")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{20} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 1), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_12(Scene):
    def construct(self):
        mob0 = Rectangle(color=PURPLE)
        mob0.shift(UP * 1)
        mob1 = Star(color=YELLOW)
        mob1.shift(DOWN * 1)
        mob2 = Triangle(color=RED)
        mob2.shift(DOWN * 2)
        mob3 = Square(color=GREEN)
        mob3.shift(LEFT * 3)
        title = Text("scene 02-12 step 46")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{4} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(Create(group), run_time=1)
        self.play(mob0.animate.shift(LEFT * 2), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_13(Scene):
    def construct(self):
        mob0 = Dot(color=ORANGE)
        mob0.shift(UP * 1)
        mob1 = Circle(color=ORANGE)
        mob1.shift(UP * 2)
        mob2 = Star(color=ORANGE)
        mob2.shift(LEFT * 2)
        mob3 = Dot(color=ORANGE)
        mob3.shift(UP * 2)
        title = Text("scene 02-13 step 74")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{19} k^4")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(GrowFromCenter(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 2), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 1), run_time=1)
        self.play(mob3.animate.shift(RIGHT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_14(Scene):
    def construct(self):
        mob0 = Triangle(color=GREEN)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=PURPLE)
        mob1.shift(LEFT * 2)
        mob2 = Triangle(color=BLUE)
        mob2.shift(UP * 3)
        title = Text("scene 02-14 step 66")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{14} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=2)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_15(Scene):
    def construct(self):
        mob0 = Rectangle(color=BLUE)
        mob0.shift(UP * 2)
        mob1 = Triangle(color=GREEN)
        mob1.shift(DOWN * 3)
        mob2 = Dot(color=RED)
        mob2.shift(DOWN * 3)
        mob3 = Rectangle(color=GREEN)
        mob3.shift(DOWN * 1)
        title = Text("scene 02-15 step 13")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{18} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 1), run_time=1)
        self.play(mob2.animate.shift(DOWN * 1), run_time=1)
        self.play(mob3.animate.shift(LEFT * 1), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_16(Scene):
    def construct(self):
        mob0 = Triangle(color=ORANGE)
        mob0.shift(RIGHT * 3)
        mob1 = Square(color=PURPLE)
        mob1.shift(UP * 3)
        mob2 = Rectangle(color=PURPLE)
        mob2.shift(DOWN * 2)
        title = Text("scene 02-16 step 38")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob1.animate.shift(UP * 1), run_time=2)
        self.play(mob2.animate.shift(UP * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_17(Scene):
    def construct(self):
        mob0 = Circle(color=GREEN)
        mob0.shift(RIGHT * 3)
        mob1 = Circle(color=ORANGE)
        mob1.shift(DOWN * 1)
        mob2 = Dot(color=YELLOW)
        mob2.shift(UP * 3)
        title = Text("scene 02-17 step 49")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{6} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(DrawBorderThenFill(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 1), run_time=1)
        self.play(mob1.animate.shift(LEFT * 2), run_time=1)
        self.play(mob2.animate.shift(LEFT * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_18(Scene):
    def construct(self):
        mob0 = Square(color=GREEN)
        mob0.shift(DOWN * 2)
        mob1 = Star(color=PURPLE)
        mob1.shift(RIGHT * 1)
        mob2 = Square(color=RED)
        mob2.shift(DOWN * 1)
        mob3 = Square(color=PURPLE)
        mob3.shift(DOWN * 3)
        title = Text("scene 02-18 step 26")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{17} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=3)
        self.play(mob0.animate.shift(DOWN * 2), run_time=1)
        self.play(mob1.animate.shift(UP * 2), run_time=2)
        self.play(mob2.animate.shift(LEFT * 2), run_time=2)
        self.play(mob3.animate.shift(UP * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_19(Scene):
    def construct(self):
        mob0 = Circle(color=RED)
        mob0.shift(DOWN * 3)
        mob1 = Square(color=BLUE)
        mob1.shift(UP * 2)
        mob2 = Circle(color=YELLOW)
        mob2.shift(UP * 1)
        mob3 = Square(color=RED)
        mob3.shift(DOWN * 2)
        title = Text("scene 02-19 step 97")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{8} k^2")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2, mob3)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(DOWN * 1), run_time=1)
        self.play(mob2.animate.shift(RIGHT * 2), run_time=2)
        self.play(mob3.animate.shift(DOWN * 2), run_time=2)
        self.play(Transform(mob0, mob1), run_time=1)
        self.wait(1)
        self.play(Uncreate(group), FadeOut(title), FadeOut(formula))
        self.wait(1)


class Scene02_20(Scene):
    def construct(self):
        mob0 = Square(color=BLUE)
        mob0.shift(LEFT * 1)
        mob1 = Circle(color=BLUE)
        mob1.shift(DOWN * 1)
        mob2 = Star(color=RED)
        mob2.shift(RIGHT * 1)
        title = Text("scene 02-20 step 97")
        title.to_edge(UP)
        formula = MathTex(r"\sum_{k=1}^{16} k^3")
        formula.next_to(title, DOWN)
        group = VGroup(mob0, mob1, mob2)
        self.add(title, formula)
        self.play(FadeIn(group), run_time=2)
        self.play(mob0.animate.shift(LEFT * 1), run_time=1)
        self.play(mob1.animate.shift(RIGHT * 1), run_time=2)
        self.play(mob2.animate.shift(DOWN * 2), run_time=1)
        self.play(Transform(mob0, mob1), run_time=2)
        self.wait(2)
        self.play(FadeOut(group), FadeOut(title), FadeOut(formula))
        self.wait(1)
